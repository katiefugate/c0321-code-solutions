/* exported omit */

// function omit(source, keys) {
//   var copy = Object.assign({}, source);

//   for (var k = 0; k < keys.length; k++) {
//       if (key in copy === keys[k]) {
//         delete copy[keys[k]];
//       }
//   }
//   return copy;
// }
